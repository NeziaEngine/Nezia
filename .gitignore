target/
# サンドボックス環境で ~/.cargo が書込不可のときに使うプロジェクトローカル CARGO_HOME
.cargo-home/
*.swp
*.swo
.DS_Store
.idea/
Sandbox/

# csbindgen が build.rs で再生成する成果物（CI でのみ artifact 化する）
crates/ffi/bindings/

# 個人作業メモ (quiz スキル等の出力先)
docs/quiz/
